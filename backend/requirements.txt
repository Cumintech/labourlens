fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
python-dotenv==1.0.1
cryptography==44.0.0
pyjwt==2.10.1
bcrypt==4.2.1
python-multipart==0.0.20
psycopg2-binary==2.9.10
pydantic[email]==2.10.4

# Aadhaar OCR (see ocr.py for why EasyOCR over Tesseract on this
# machine). Heavy (PyTorch-based) -- revisit if this ends up deployed
# somewhere Tesseract's system binary is easy to install (any normal
# Linux host, unlike this Windows dev machine).
easyocr==1.7.2
