fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
alembic==1.19.2
python-dotenv==1.0.1
cryptography==44.0.0
pyjwt==2.10.1
bcrypt==4.2.1
python-multipart==0.0.20
psycopg2-binary==2.9.10
pydantic[email]==2.10.4

# Worker ID-card photo storage (see photo_storage.py) -- talks to
# Supabase Storage's plain REST API directly rather than pulling in the
# full supabase-py SDK, which drags in its own realtime/postgrest/auth
# clients this project has no use for. Was already resolvable
# transitively (verify_*.py scripts already `import requests`), pinned
# explicitly now that backend runtime code depends on it directly too.
requests==2.34.2

# Aadhaar OCR (see ocr.py for why EasyOCR over Tesseract on this
# machine). Heavy (PyTorch-based) -- revisit if this ends up deployed
# somewhere Tesseract's system binary is easy to install (any normal
# Linux host, unlike this Windows dev machine).
easyocr==1.7.2

# Portal Sync Worker (see sync_worker.py) -- the Portal has no API, so
# this drives it the same way a person would. `playwright install
# chromium` also needs to run once wherever this deploys (not just pip
# install -- that only gets the Python bindings, not the browser binary).
playwright==1.62.0

# Day 4 reports (see reports.py) -- both pure-Python/pip-installable, no
# system binary (same reasoning as EasyOCR over Tesseract elsewhere).
openpyxl==3.1.5
reportlab==4.2.5

# Tamil text on Form 25/25-B (see forms.py) -- ReportLab's own TrueType
# font embedding has a confirmed glyph-substitution bug for this font
# (verified by extracting the embedded subset font and comparing glyph
# outlines byte-for-byte against the original -- ReportLab silently
# embeds the wrong glyph for some codepoints). Tamil labels are shaped
# with HarfBuzz and rasterized with FreeType into images instead of
# being drawn as ReportLab native text, which sidesteps that bug
# entirely and also gets correct script shaping (vowel-sign reordering)
# for free, which ReportLab never does regardless of the font bug.
uharfbuzz==0.56.1
freetype-py==2.5.1

# Biometric attendance sync (see biometric.py) -- deliberately NOT
# installed today. ZKTecoConnector imports it lazily inside a try/except
# specifically so nothing here needs it: every real usage right now is
# MockConnector (BIOMETRIC_CONNECTOR unset/"mock"), and pyzk needs a real
# device on the network to do anything meaningful anyway. Uncomment and
# pip install once a physical ZKTeco unit is available to test against,
# then set BIOMETRIC_CONNECTOR=zkteco.
# pyzk==0.9
